import { ClazzDirective } from './clazz.directive';

describe('ClazzDirective', () => {
  it('should create an instance', () => {
    const directive = new ClazzDirective();
    expect(directive).toBeTruthy();
  });
});
