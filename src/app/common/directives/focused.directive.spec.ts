import { FocusedDirective } from './focused.directive';

describe('FocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusedDirective();
    expect(directive).toBeTruthy();
  });
});
