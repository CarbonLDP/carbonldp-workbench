import { Component } from "@angular/core";

import "semantic-ui/semantic";

@Component( {
	selector: "cw-apps-catalog-view",
	templateUrl: "./apps-catalog.view.html",
	styles: [ ":host { display: block; }" ],
} )
export class AppsCatalogView {
}

