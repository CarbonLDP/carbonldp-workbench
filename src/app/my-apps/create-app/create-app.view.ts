import { Component } from "@angular/core";

import "semantic-ui/semantic";

@Component( {
	selector: "cw-create-app-view",
	templateUrl: "./create-app.view.html",
	styles: [ ":host { display: block; }" ],
} )

export class CreateAppView {
}

